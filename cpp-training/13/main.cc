#include <iostream>
#include <string>

using namespace std;

int main() {
  string t;
  scanf("%s", &t);
  cout << t << endl;

  /*
  while (scanf(" %s", &t) == 1) {
    printf("%s\n", t.c_str());
  };
  */
}